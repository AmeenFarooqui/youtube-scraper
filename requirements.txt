# Core dependency — the YouTube data engine.
# [default] pulls the companion packages (yt-dlp-ejs for the required JS runtime
# support, etc.). No upper pin: YouTube breaks old extractors constantly, so
# refresh with:  pip install -U -r requirements.txt
yt-dlp[default]>=2026.7.4

# Rich terminal output (colored tables, progress bars, panels)
rich>=13.0.0

# Progress bars for batch operations
tqdm>=4.66.0


# Sentiment analysis for YouTube comments (VADER — tuned for social media text)
vaderSentiment>=3.3.2

# Note: ffmpeg must be installed separately (not a Python package)
# Linux: sudo apt install ffmpeg
# Mac:   brew install ffmpeg
# Windows: https://ffmpeg.org/download.html
